<!-- Bring in EB Garamond font -->
<!-- nav always horizontal due to mobile overlap -->
<link href="https://fonts.googleapis.com/css2?family=EB+Garamond&display=swap" rel="stylesheet">
<style>
body {
  font-family: 'EB Garamond', Georgia, 'Times New Roman', serif;
}
.site-nav {
  display: flex !important;
  flex-wrap: wrap;
  gap: 1rem;
  justify-content: center;
}
.site-title {
  flex-basis: 100%;
  margin-bottom: 0.5rem;
  text-align: left;
}

.site-header .menu-icon,
.site-header .trigger {
  display: none !important;
}
@media (max-width: 768px) {
  .site-header {
    padding-top: 1rem;
  }

  .site-nav {
    flex-direction: column;
    align-items: center;
    text-align: center;
    padding: 1rem 0;
  }

  .site-nav a {
    display: block;
    width: 100%;
    padding: 0.5rem 0;
  }

  .site-title {
    font-weight: bold;
    font-size: 1.5rem;
    text-align: center;
    margin-bottom: 1rem;
    border-bottom: 1px solid #ccc;
    padding-bottom: 0.5rem;
    width: 100%;
  }
}
</style>

<!-- Re-implement Minima's default header -->
<header class="site-header" role="banner">
  <div class="wrapper">
    <a class="site-title" rel="author" href="{{ '/' | relative_url }}">{{ site.title | escape }}</a>

    <nav class="site-nav">
      <a class="page-link" href="/index.html#blog">Blog Posts</a>
      {% for page in site.pages %}
        {% unless page.permalink == "/" or page.nav_exclude %}
          <a class="page-link" href="{{ page.url | relative_url }}">{{ page.title }}</a>
        {% endunless %}
      {% endfor %}
    </nav>
  </div>
</header>
